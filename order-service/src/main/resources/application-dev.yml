# Uses env vars for Docker compatibility (DB_HOST=host.docker.internal when in containers)
spring:
  r2dbc:
    url: r2dbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:inventory}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  flyway:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:inventory}
    user: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}

client:
  product-service:
    url: ${PRODUCT_SERVICE_URL:http://localhost:8081}
  inventory-service:
    url: ${INVENTORY_SERVICE_URL:http://localhost:8081}

logging:
  level:
    com.quickcommerce: DEBUG
    org.springframework.r2dbc: DEBUG
    io.r2dbc.mysql: DEBUG

# Fast failsafe for local dev — payment confirms ~5s after /pay is called
payment:
  failsafe-interval-ms: 3000    # Run every 3 seconds
  failsafe-cutoff-seconds: 2    # Check attempts idle for >2s
